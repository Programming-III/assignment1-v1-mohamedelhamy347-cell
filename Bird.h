#ifndef BIRD_H
#define BIRD_H

#include "Animal.h"

class Bird::Animal{
Private float wingSpan;
public:
Bird()::Public Animal();
Bird(float wingSpan)::Animal(string name, int age, bool isHungry);
   ~Bird(); 
    
    
}







#endif
